---
title: 'Code'
order: 2
---

## Import

Import **the container** scss files.

```scss
@import 'layouts/_l.container';
```

## Usage

### Default container

To use a **default container** in your code, simply add the `ml-container` class to the HTML element of your choice, as follows:

```html
<main class="ml-container">
  ...
</main>
```

### Fluid container

If you want to use only the default paddings, but still want the container to be fluid at any viewport width, use the `ml-container--fluid` modifier.

```html
<main class="ml-container ml-container--fluid">
  ...
</main>
```

## Related documentation

To see container in action, all examples of [the grid system documentation](/Foundations/Layout/Grid/code/) uses it.
