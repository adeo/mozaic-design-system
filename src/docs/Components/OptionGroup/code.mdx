---
title: 'Code'
order: 2
---

## Import

Import the settings and the option-group `scss` files for the default version.

```scss
@import 'settings-tools/_all-settings';
@import '_c.option-group'; // mandatory
```

Import the settings and the option-group-extended `scss` files for the extended version.

```scss
@import 'settings-tools/_all-settings';
@import '_c.option-group-extended'; // mandatory
```

## Usage

The creation of a default option-group component in your code is done using the following HTML structure:

```HTML
  <div class="mc-option-group">
		<input type="radio" name="option-group" value="radio-01" id="radio-01" class="mc-option-group__input" checked/>
    <label class="mc-option-group__label" for="radio-01"> Checked </label>
		<input type="radio" name="option-group" value="radio-02" id="radio-02" class="mc-option-group__input" />
    <label class="mc-option-group__label" for="radio-02"> Content </label>
		<input disabled type="radio" name="option-group" value="disabled" id="radio-03" class="mc-option-group__input" />
    <label class="mc-option-group__label" for="radio-03"> Disabled </label>
  </div>
```

The creation of a extended option-group component in your code is done using the following HTML structure:

```HTML
  <div class="mc-option-group-extended">
    <div class="mc-option-group-extended__card">
      <input type="radio" name="option-group" value="1" id="radio-01" class="mc-radio__input mc-option-group-extended__input" aria-label="radio-01"/>
      <label for="radio-01" class="mc-option-group-extended__label"></label>
      <div class="mc-option-group__content"></div>
		</div>
    <div class="mc-option-group-extended__card">
      <input type="radio" name="option-group" value="2" id="radio-02" class="mc-radio__input mc-option-group-extended__input" aria-label="radio-02"/>
      <label for="radio-02" class="mc-option-group-extended__label"></label>
      <div class="mc-option-group-extended__content"></div>
    </div>
  </div>
```


## Variations

### Default

The default version of the option-group component can only contain text element.
This variation is single choice only, you must use radio buttons inside the component.
<Preview path="default" />


### Extended

#### Single option

In order to use the extended option-group with a single option you must use radio buttons inside the component.
<Preview path="extended" />
#### Multi options

In order to use the extended option-group with a multi options you must use checkboxes inside the component.
<Preview path="checkbox" />

#### Centered

In some cases, the radio or the checkbox has to be centered.

To achieve that you have to add `mc-option-group-extended--centered` modifier to the `mc-option-group-extended` element. 
<Preview path="centered" />

#### Padding
In some cases, the radio or the checkbox needs a smaller padding (default: 16px).

To achieve that you have to add `mc-option-group-extended--spaced` modifier to the `mc-option-group-extended` element.
<Preview path="spaced" />
